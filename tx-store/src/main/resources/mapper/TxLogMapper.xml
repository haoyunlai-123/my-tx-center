<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.my.tx.store.mapper.TxLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tx_log(tx_id, step_index, phase, success, cost_ms, message)
        VALUES (#{txId}, #{stepIndex}, #{phase}, #{success}, #{costMs}, #{message})
    </insert>

    <select id="selectByTxId" resultType="com.my.tx.store.model.TxLogDO">
        SELECT id, tx_id AS txId, step_index AS stepIndex, phase, success, cost_ms AS costMs, message, created_at AS createdAt
        FROM tx_log
        WHERE tx_id = #{txId}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

</mapper>